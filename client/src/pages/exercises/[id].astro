---
import Layout from "../../layouts/Layout.astro";
import Exercise from "../../components/Exercise.svelte";
export const prerender = false;

const { id } = Astro.params;
---

<Layout title={`Exercise ${id}`}>
  <Exercise client:idle exerciseId={id}/>
</Layout>
